import { HeaderConfig } from "./HeaderConfig";

export const SupplierSection = () => {
  return (
    <section className="w-full mx-auto max-w-5xl">
      <HeaderConfig placeholderInput="Buscar proveedor" buttonText="Nuevo Proveedor" />
    </section>
  );
};
